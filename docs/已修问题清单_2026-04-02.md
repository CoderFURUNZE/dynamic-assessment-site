# 已修问题清单（2026-04-02）

## 1. 路由与页面状态

- 修复教师、管理员“登录后无法回到上次页面”
- 修复页面标题未随路由切换同步更新

## 2. 图谱与内容建设

- 修复教师图谱页顶部统计受“是否展开节点”影响，导致知识点数量显示错误
- 修复教师知识图谱页在 Chrome 中出现“统计有数据但画布空白”的渲染问题
- 原因是超大 SVG 画布整体缩放平移导致浏览器不绘制
- 已改为固定视口 SVG + 内层分组缩放平移
- 为 `测试课程` 补齐最小演示图谱数据：`5` 个知识点、`4` 条关系

## 3. 阶段评价与画像

- 修复阶段画像汇总缺少 `_clamp01 / _risk_level`，导致画像汇总接口异常
- 修复教师导入页缺少 `quiz` 类型，前后端导入能力不一致
- 修复学生报告页在 `portrait_dimensions` 为空时误判为“没有阶段结果”

## 4. 报名与课程状态

- 修复 `draft` 课程仍可被学生报名
- 修复课程归档后仍可继续通过报名申请
- 修复课程已结束后学生仍可继续进入学习入口
- 修复删除带有关联报名/评价数据的课程时直接报 `500`
- 当前已改为明确返回 `400`，阻止删除

## 5. 教师评分与审核

- 修复教师最终评分页会把未选课学生带入名单
- 修复未产生任何阶段评价数据的学生仍可被教师确认期末分
- 修复教师最终评分页直达链接时会丢失 `user_id`
- 修复教师评语与学生报告展示链路中的阶段空态误判

## 6. 当前非代码性说明

- 学生端 `测试课程` 图谱页之前为空，根因是课程内容数据缺失，不是前端渲染 bug
- 现已补齐最小图谱数据，可用于演示
- 学生图谱页默认不会自动展开知识点，答辩时点击“展开全部节点”即可
