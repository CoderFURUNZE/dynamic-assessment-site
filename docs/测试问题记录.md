# 测试问题记录（你测试后直接填写）

## 使用说明
1. 一条问题写一行。  
2. 优先填 `P0/P1`，我会先修阻塞问题。  
3. 如果有截图，把截图文件名写到“附件”列。  

| 编号 | 日期 | 优先级 | 角色 | 页面/路径 | 复现步骤 | 实际结果 | 期望结果 | 报错信息 | 附件 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|
| BUG-001 |  | P1 | 教师 | /teacher/graph-workspace | 1. 进入图谱 2. 点击节点 3. 保存关系 | 边数量多时画布与后端不一致 | 列表一次拉全 | 已将 `GET /admin/edges` 单页上限调至 500，与前端请求一致 |  | 已修复（代码） |
| BUG-002 |  | P2 | 学生/教师 | /student/graph-workspace、教师图谱 | 1. 搜索过滤知识点 2. 已选节点不在过滤结果中 | 画布上当前节点消失，无法再点选 | 保留选中节点于画布 | `visibleKps` 始终并入当前选中 KP |  | 已修复（代码） |

---

## 修复回执（我来填写）

| 编号 | 原因分析 | 修复内容 | 影响范围 | 提交时间 | 回归结果 |
|---|---|---|---|---|---|
| BUG-002 | 搜索后 `filteredKps` 不含当前选中项，`visibleKps` 未包含该节点 | 计算 `visibleKps` 时若选中为 KP 且不在列表则并入 | 学生 `KnowledgeGraphWorkspace`、教师 `TeacherGraphWorkbench` |  | 待人工回归 |

